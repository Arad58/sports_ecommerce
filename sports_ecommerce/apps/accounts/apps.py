from django.apps import AppConfig

# Configuration for the account's app
class AccountsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'sports_ecommerce.apps.accounts'
